# Email Configuration for Authentication System
# This should be configured by the system administrator

# SMTP Settings for 15th Judicial Circuit Public Defender's Office
# Uses Office365/Outlook for @pd15.org and @pd15.state.fl.us domains
SMTP_SERVER=smtp-mail.outlook.com
SMTP_PORT=587

# Organization email account for sending verification emails
# This should be a dedicated service account like: dkarpay@pd15.org
SMTP_USERNAME=dkarpay@pd15.org
SMTP_PASSWORD=your-organization-email-password

# JWT Secret Key (generate a secure random key)
JWT_SECRET=your-very-secure-random-jwt-secret-key-here

# Optional: Custom domain restrictions (if different from hardcoded)
# ALLOWED_DOMAINS=@pd15.org,@pd15.state.fl.us