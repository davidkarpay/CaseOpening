# Case Opening Sheet Manager - Authentication Configuration
# Configure these settings for 15th Judicial Circuit Public Defender's Office

# Office365/Outlook SMTP Configuration
SMTP_SERVER=smtp-mail.outlook.com
SMTP_PORT=587

# Organizational service account for sending verification emails and PINs
# Example: casemanager@pd15.org or system@pd15.state.fl.us
SMTP_USERNAME=casemanager@pd15.org
SMTP_PASSWORD=your-service-account-password

# JWT Security Key (generate a long, random string)
JWT_SECRET=your-secure-jwt-secret-key-minimum-32-characters